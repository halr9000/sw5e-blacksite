{
  "name": "blacksite-archive",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "roster": "node scripts/build-roster.mjs",
    "test": "node --test",
    "dev": "npm run roster && astro dev",
    "build": "npm run roster && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.13.0"
  }
}
